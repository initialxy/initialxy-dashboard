export enum ButtonType {
  Add = 'add',
  Edit = 'edit',
  Delete = 'delete',
}

export enum ButtonPosition {
  Standalone = 'standalone',
  Top = 'top',
  Right = 'right',
  Bottom = 'bottom',
  Left = 'left',
}
